import assert from 'node:assert/strict';import fs from 'node:fs';const html=fs.readFileSync('index.html','utf8');const repos=["argentine_slang_normalizer", "braille_visualizer", "card_score_sandbox", "cookie_lab", "django_job_board", "math_toolkit", "python_app_packager", "sliding_puzzle_solver", "memory_monitor", "process_inspector", "python_practice_collection", "shortcut_auditor", "startup_inspector", "trading_card_estimator", "chance_score_game"];for(const repo of repos){assert.ok(html.includes(`https://mateotrucco.github.io/${repo}/`),`missing demo ${repo}`);assert.ok(html.includes(`https://github.com/MateoTrucco/${repo}`),`missing source ${repo}`);}console.log(`Portfolio links verified for ${repos.length} projects.`);
